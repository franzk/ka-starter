services:
  ka-keycloak:
    # Import any json file found in /opt/keycloak/data/import
    volumes:
      - ../ka-keycloak/realm:/opt/keycloak/data/import:ro
    command: ["start", "--http-enabled=true", "--proxy-headers=xforwarded", "--import-realm"]
